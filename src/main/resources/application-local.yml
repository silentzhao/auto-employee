spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:employee_manager}
    username: ${DB_USERNAME:zhaojiaqi}
    password: ${DB_PASSWORD:zjq123456}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      pool-name: employee-manager-pool
      connection-timeout: 3000

logging:
  level:
    com.baomidou.mybatisplus: info
