spring:
  datasource:
    url: jdbc:h2:mem:employee-manager;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

logging:
  level:
    root: warn
    com.aidigital.employee: info
